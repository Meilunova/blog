# 设置默认行为，如果Git认为文件是文本，则启用自动行结束符转换
* text=auto

# 明确声明你希望始终被规范化并在检出时转换为本地行结束符的文件
*.css text
*.html text
*.js text
*.json text
*.md text
*.yml text
*.yaml text
*.xml text
*.txt text

# 声明你希望始终使用LF行结束符的文件（即使在Windows上）
*.sh text eol=lf

# 声明你不希望被修改的文件
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
